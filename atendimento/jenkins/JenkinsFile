#!/usr/bin/env groovy
pipeline {
    agent any
    stages {

        stage('Build') {
            steps{
                    bat "mvn package -Dmaven.test.skip=true"
            }
        }

        stage('run') {
            steps{
                    bat "mvn spring-boot:run"
            }
        }
    }
    return
}