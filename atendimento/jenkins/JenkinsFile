#!/usr/bin/env groovy
pipeline {
    agent any
    stages {
        stage('Pre-build') {
            steps{
                bat "mvn clean package"
            }
        }
        stage('Build') {
            steps{
                    bat "mvn package -Dmaven.test.skip=true"
            }
        }

    }
}